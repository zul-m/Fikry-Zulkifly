<section class="hero" data-screen-label="01 Hero" aria-label="Hero">
  <div class="hero-inner">
    <div class="hero-grid">
      <h1 class="hero-headline">Pakar Pemasaran Hartanah</h1>
      <div class="hero-side">
        <p>
          Sedia membantu anda menguruskan proses jualan atau sewaan hartanah
          melalui pendekatan yang sistematik dan telus, bagi memastikan
          urusan aset anda berjalan dengan lancar dan teratur.
        </p>
        <div class="hero-actions">
          <a class="btn btn-primary" href="http://wasap.my/60176515784/TanyaHartanah" target="_blank" rel="noopener noreferrer">
            Tanya Hartanah
            <svg class="icon-whatsapp" width="20" height="20" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
              <path d="M12.04 2C6.58 2 2.13 6.45 2.13 11.91c0 1.75.46 3.45 1.32 4.95L2.05 22l5.28-1.38a9.9 9.9 0 0 0 4.71 1.2h.01c5.46 0 9.9-4.45 9.9-9.91C21.96 6.45 17.5 2 12.04 2Zm5.8 14.09c-.24.68-1.4 1.3-1.93 1.38-.5.08-1.12.11-1.8-.11-.42-.13-.95-.31-1.64-.6-2.9-1.25-4.79-4.17-4.94-4.36-.14-.2-1.18-1.57-1.18-3 0-1.42.75-2.12 1.02-2.4.26-.29.58-.36.77-.36.19 0 .39 0 .55.01.18.01.42-.07.65.5.24.58.82 2 .89 2.15.07.14.12.31.02.5-.1.19-.15.31-.29.48-.15.17-.31.38-.44.5-.15.14-.3.3-.13.58.17.29.76 1.25 1.63 2.02 1.12.99 2.06 1.3 2.35 1.45.29.14.46.12.63-.07.17-.19.72-.84.92-1.13.19-.29.38-.24.64-.14.26.1 1.66.78 1.95.93.29.14.48.21.55.33.07.12.07.68-.17 1.36Z"/>
            </svg>
          </a>
        </div>
      </div>
    </div>
    <article class="id-card" aria-label="Mengenai Fikry Zulkifly">
      <p class="id-blurb">
        Saya seorang Perunding Hartanah berdaftar
        <b>(REN&nbsp;71735)</b> dengan pengalaman selama 5&nbsp;tahun, dan
        telah berjaya membantu pemilik menyewakan serta menjual
        <b>lebih daripada 100&nbsp;unit hartanah</b>!
      </p>
      <h2 class="id-name">Fikry Zulkifly</h2>
      <div class="id-portrait-slot" aria-hidden="true"></div>
      <div
        class="id-portrait"
        role="img"
        aria-label="Potret Fikry Zulkifly"
      >
      </div>
    </article>
  </div>
</section>

<style>
  .hero {
    position: relative;
    width: 100%;
    min-height: 100dvh;
    border-radius: 0 0 var(--radius-main) var(--radius-main);
    overflow: hidden;
    background: var(--color-dark-900);
    isolation: isolate;
  }
  .hero::before {
    content: "";
    position: absolute;
    inset: 0;
    z-index: -1;
    background:
      linear-gradient(
        180deg,
        color-mix(in srgb, var(--color-brand-800) 55%, transparent) 0%,
        color-mix(in srgb, var(--color-dark-900) 78%, transparent) 100%
      ),
      url("/images/modern-house.png") center / cover no-repeat;
    animation: hero-bg-zoom 2s cubic-bezier(0.25, 0.46, 0.45, 0.94) forwards;
  }
  @keyframes hero-bg-zoom {
    from { transform: scale(1.08); }
    to   { transform: scale(1); }
  }
  @media (prefers-reduced-motion: reduce) {
    .hero::before {
      animation: none;
    }
  }
  .hero-inner {
    position: relative;
    max-width: var(--max-width-main);
    margin-inline: auto;
    min-height: 100dvh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding-block: calc(var(--space-8) + var(--space-8)) var(--space-7);
    padding-inline: var(--margin);
    color: var(--color-light-100);
  }
  .hero-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-6);
    align-items: start;
  }
  .hero-headline {
    font-family: var(--font-family);
    font-weight: var(--font-weight-medium);
    font-size: var(--font-size-h1);
    line-height: var(--line-height-small);
    letter-spacing: var(--letter-spacing-tight);
    color: var(--color-light-100);
    max-width: var(--max-width-small);
    margin: 0;
  }
  .hero-side {
    display: flex;
    flex-direction: column;
    gap: var(--space-5);
    padding-top: var(--space-1);
  }
  .hero-side p {
    font-family: var(--font-family);
    font-size: var(--font-size-large);
    line-height: var(--line-height-large);
    color: var(--color-light-100);
    max-width: var(--max-width-small);
  }
  .hero-actions {
    display: flex;
    gap: var(--space-3);
    flex-wrap: wrap;
  }
  .id-card {
    display: none;
    position: relative;
    margin-top: calc(var(--space-8) * 6);
    background: var(--color-light-100);
    color: var(--color-dark-900);
    border-radius: var(--radius-main);
    grid-template-columns: minmax(0, 1.55fr) minmax(0, 1fr) clamp(9rem, 13vw, 12rem);
    gap: var(--space-7);
    align-items: center;
    padding: var(--space-5) var(--space-6);
    box-shadow: 0 var(--space-5) var(--space-7)
      color-mix(in srgb, var(--color-dark-900) 18%, transparent);
  }
  .id-blurb {
    font-family: var(--font-family);
    font-size: var(--font-size-body);
    line-height: var(--line-height-large);
    color: var(--color-dark-900);
    max-width: var(--max-width-small);
  }
  .id-blurb b {
    font-weight: var(--font-weight-bold);
    color: inherit;
  }
  .id-name {
    font-family: var(--font-family);
    font-weight: var(--font-weight-bold);
    font-size: var(--font-size-h3);
    line-height: var(--line-height-small);
    letter-spacing: var(--letter-spacing-tight);
    color: var(--color-dark-900);
    margin: 0;
    text-align: center;
  }
  .id-portrait {
    position: absolute;
    right: var(--space-6);
    bottom: 0;
    width: clamp(9rem, 13vw, 12rem);
    aspect-ratio: 642 / 1039;
    border-radius: var(--radius-main);
    overflow: hidden;
    background: url("/images/FZ.webp") center / cover no-repeat;
  }
  .id-portrait-slot {
    align-self: stretch;
  }

  @media (min-width: 80rem) {
    .hero-grid {
      grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
      gap: var(--gutter);
    }
    .id-card { display: grid; }
  }
</style>
